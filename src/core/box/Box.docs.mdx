import { Meta, Primary } from '@storybook/blocks';
import * as BoxStories from './Box.stories.js';

<Meta of={BoxStories} />

# Box

### Use the `Box` component as a layout primitive to add margin, padding, and colors to content.

<Primary />
